#include <stdio.h>

/**
 * main - Print Alphabet Backwards.
 * Return: Returns 0 if no errors
 */

int main(void)
{
	char ab;

	for (ab = 'z'; ab >= 'a'; a--)
		printf(ab);

	printf('\n');
	return (0);
}
